export class RegisterClientCatalog {
    constructor({ 
        states, 
        towns, 
        zones, 
        daysOfRoutes 
    }) {
        this.states = states;
        this.towns = towns;
        this.zones = zones;
        this.daysOfRoutes = daysOfRoutes;
    }
}